# script.py
import argparse
import os
from datetime import datetime

parser = argparse.ArgumentParser()
parser.add_argument('--x', type=float, required=True, help='Input number to square')
args = parser.parse_args()

result = args.x ** 2
timestamp = datetime.now().strftime("%Y-%m-%d %H:%M:%S")

log_line = f"[{timestamp}] The square of {args.x} is {result}\n"
print(log_line)

# Optional: also write to a log file inside script (not necessary if you already capture via nohup)
# with open('logs/manual_log.txt', 'a') as f:
#     f.write(log_line)
